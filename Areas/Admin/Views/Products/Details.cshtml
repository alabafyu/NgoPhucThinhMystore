@model NgoPhucThinhMystore.Models.Product

@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/Adminlayout.cshtml";
}
<head>
    <style>
        img {
            height: 250px;
            max-width: 250px;
        }
    </style>
</head>
<h2 class="bg-success">Chi tiết Sản Phẩm</h2>

@Html.Partial("PVProductInfo", Model)

<div class="form-group">
    <div class="col-offset-2 col-md-10">
        @Html.ActionLink("Sữa", "Edit", new { id = Model.ProductID }, new { @class = "btn btn-primary btn-sm" }) |
        @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-primary btn-sm" })
    </div>

</div>
